---
apiVersion: v1
kind: ConfigMap
metadata:
  name: new-api-config
  namespace: {{ .Values.namespace.name }}
  labels:
    app: new-api
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
