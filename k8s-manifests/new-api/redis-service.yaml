apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: new-api
  labels:
    app: new-api
    component: redis
spec:
  type: ClusterIP
  selector:
    app: new-api
    component: redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
