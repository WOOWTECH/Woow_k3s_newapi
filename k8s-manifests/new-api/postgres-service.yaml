apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: new-api
  labels:
    app: new-api
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: new-api
    component: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
